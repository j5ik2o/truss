include classpath("common/akka.conf")
include classpath("local/akka.conf")
include classpath("common/truss.conf")

akka {
  grpc.client {
    "truss.interfaceAdaptor.grpc.proto.WalletGRPCService" = {
      host = "localhost"
      port = 18080
      user-agent = "Akka-gRPC"
      use-tls = false
    }
  }
}
