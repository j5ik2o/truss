truss.api-server {
  rest {
    host = "0.0.0.0"
    port = 18080
    port = ${?TRUSS_API_SERVER_REST_PORT}
  }
  grpc {
    host = "0.0.0.0"
    port = 18081
    port = ${?TRUSS_API_SERVER_GRPC_PORT}
  }
  grpc-web {
    host = "0.0.0.0"
    port = 18082
    port = ${?TRUSS_API_SERVER_GRPC_WEB_PORT}
  }
  terminate.duration = 1s
}
